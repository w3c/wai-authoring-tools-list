<article class="tool" id="{{ tool.id }}">
  <h3>{{ tool.name }} <span>by {{ tool.vendor }}</span> <a href="#" class="tag">{{ tool.type_of_tool }}</a></h3>
  <p>{{ tool.description }}</p>
  <details>
    <summary class="button button-secondary">Details</summary>
    <dl class="tool__features">
      <dt>License</dt>
      <dd>{{ tool.license }}</dd>
      <dt>Version</dt>
      <dd>{{ tool.version }}</dd>
      <dt>Submitted</dt>
      <dd>{{ tool.date_submitted }}</dd>
      <dt>Released</dt>
      <dd>{{ tool.date_released }}</dd>
      {% if tool.website %}
      <dt>Website</dt>
      <dd><a href="{{ tool.website }}" rel="external">{{ tool.website }}</a></dd>
      {% endif %}
      <dt>Accessibility statement</dt>
      <dd>{{ tool.accessibility_statement | default: 'Not available' }}</dd>
      <dt class="tool__features-a11y">Accessibility features <small>(See <a href="selecting#editing-experience-for-content-editors">list</a>)</small></dt>
      <dd class="tool__features-a11y">
        <ul>
      {% for feature in tool.conforms_to %}
          {% if feature == 'works-with-keyboard' %}
          <li><strong>{{ feature }}</strong> (partly)<br>
          Should mostly work, except for the video editor, we plan to release that in version 3.</li> 
          {% else %}
          <li><strong>{{ feature }}</strong></li>
          {% endif %}
      {% endfor %}
        </ul>
      </dd>
    </dl>
  </details>
</article>
